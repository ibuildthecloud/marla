package docker

//func (m *Manager) PrepareRoot(ctx context.Context, container model.Container, prepare RootCallback) (image.ID, error) {
//	rwLayer, err := m.layerStore.CreateRWLayer(container.ID(), layerID, container.MountLabel, daemon.setupInitLayer, container.HostConfig.StorageOpt)
//}
